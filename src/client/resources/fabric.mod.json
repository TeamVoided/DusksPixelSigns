{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [ "${authors}" ],
  "entrypoints": {
    "client": [
      {
        "adapter": "kotlin",
        "value": "org.teamvoided.dusks_pixel_signs.client.DusksPixelSigns::init"
      }
    ]
  },
  "mixins": [
    {
      "config": "dusks_pixel_signs.client.mixins.json",
      "environment": "client"
    }
  ],
  "accessWidener": "dusks_pixel_signs.classtweaker",
  "depends": {
    "java": ">=${java}",
    "minecraft": ">=${min_minecraft} <${max_minecraft}",
    "fabricloader": "*",
    "fabric-api": "*",
    "fabric-language-kotlin": ">=${fabric_kotlin}",
    "fzzy_config": ">=${fzzy_config}"
  },
  "contact": {
    "homepage": "https://github.com/${github_repo}",
    "issues": "https://github.com/${github_repo}/issues",
    "sources": "https://github.com/${github_repo}",
    "discord": "${discord_invite}"
  },
  "license": "${license}",
  "icon": "assets/dusks_pixel_signs/icon.png"
}